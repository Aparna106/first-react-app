// PascalCasing - Cap the first letter of all words

function Message() {
    //JSX - Javascript HTML
    const name = "Aparna";
    if (name)
        return (
            <h1>Hello, {name}!</h1>
        );
    else
        return (
            <h1>Hello, World!</h1>
        );
}

export default Message;
